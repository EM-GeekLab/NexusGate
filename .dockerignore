.git

# dependencies (bun install)
**/node_modules

# IntelliJ based IDEs
.idea
**/*.iml

# Finder (MacOS) folder config
.DS_Store

.vscode

# Some other stuff that should not be pushed to remote
.local

.env

# Build outputs (excluded locally; CI pre-builds frontend & docs then
# removes these lines via sed to include them in the Docker context)
frontend/dist/
docs/dist/
backend/out/
**/.turbo/

# Docs build output (generated, should not leak from local)
backend/docs/