---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nexusgateapps.gateway.nexusgate.io
spec:
  group: gateway.nexusgate.io
  names:
    kind: NexusGateApp
    listKind: NexusGateAppList
    plural: nexusgateapps
    singular: nexusgateapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.appName
      name: App
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.secretRef.name
      name: Secret
      type: string
    - jsonPath: .status.secretSynced
      name: Synced
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NexusGateApp is the Schema for the nexusgateapps API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NexusGateApp
            properties:
              appName:
                description: |-
                  AppName is the application name used as identifier in NexusGate
                  This will be stored in the API key's externalId field as k8s/{cluster}/{namespace}/{appName}
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              deletionPolicy:
                default: Revoke
                description: DeletionPolicy defines what happens to the API key when
                  this resource is deleted
                enum:
                - Revoke
                - Retain
                type: string
              secretRef:
                description: SecretRef defines where to store the API key
                properties:
                  key:
                    default: NEXUSGATE_API_KEY
                    description: Key is the key in the Secret data where the API key
                      will be stored
                    type: string
                  name:
                    description: Name is the name of the Secret
                    minLength: 1
                    type: string
                  namespace:
                    description: Namespace is the namespace of the Secret (defaults
                      to the NexusGateApp's namespace)
                    type: string
                required:
                - name
                type: object
            required:
            - appName
            - secretRef
            type: object
          status:
            description: status defines the observed state of NexusGateApp
            properties:
              apiKeyId:
                description: APIKeyID is the ID of the API key in NexusGate database
                type: integer
              apiKeyPrefix:
                description: APIKeyPrefix is the masked prefix of the API key (e.g.,
                  sk-xxxx...xxxx)
                type: string
              conditions:
                description: Conditions represent the current state of the NexusGateApp
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastSyncTime:
                description: LastSyncTime is the last time the resource was successfully
                  synced
                format: date-time
                type: string
              message:
                description: Message provides human-readable status information
                type: string
              phase:
                description: Phase indicates the current phase of the NexusGateApp
                enum:
                - Pending
                - Ready
                - Error
                - Deleting
                type: string
              secretSynced:
                description: SecretSynced indicates whether the Secret has been successfully
                  synced
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
